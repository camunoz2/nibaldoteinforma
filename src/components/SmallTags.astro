---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div
  class="flex font-light text-xs uppercase text-center divide-x-2 divide-double py-2"
>
  <p class="px-2">
    En <span class="uppercase underline"
      ><a href={`/categorias/${tags[0]}`}>{tags[0]}</a></span
    >
  </p>
</div>
